// Le StateGraph LangGraph (nodes, routage, checkpointer) est implémenté en
// Phase 5. Ce fichier existe pour que le workspace @kenza/agent soit valide
// dès la Phase 3 (résolution npm workspaces dans le Dockerfile de l'API).
export const AGENT_PACKAGE_READY = false;
