{
  "name": "kenza",
  "private": true,
  "version": "0.1.0",
  "description": "Kenza — agent commercial autonome (Numeos Hack 2026, Sujet 02)",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "seed": "tsx scripts/seed.ts",
    "db:migrate": "tsx packages/db/src/migrate.ts",
    "dev:api": "npm run dev -w apps/api",
    "dev:web": "npm run dev -w apps/web",
    "replay": "tsx scripts/replay-conversations.ts"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "tsx": "^4.19.1",
    "@types/node": "^22.7.5",
    "dotenv": "^16.4.5"
  }
}
